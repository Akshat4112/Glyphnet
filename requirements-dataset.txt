# Dependencies for publishing the dataset to the Hugging Face Hub
# (code/build_hf_dataset.py). Kept separate from requirements.txt because these
# are modern packages that would conflict with the pinned TF 2.9 training stack.
datasets==5.0.0
huggingface_hub==1.22.0
Pillow==9.2.0
pandas==1.4.3
